<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.astra.koksharov.astralightorganizer">
    <!-- In the client app's manifest -->
    <uses-permission android:name="android.permission.READ_USER_DICTIONARY" />
    <uses-permission android:name="android.permission.WRITE_USER_DICTIONARY" />
    <uses-permission android:name="com.astra.koksharov.organizerdb.OrganizerContentProvider.READ_USER_DICTIONARY" />
    <uses-permission android:name="com.astra.koksharov.organizerdb.OrganizerContentProvider.WRITE_USER_DICTIONARY" />
    <!-- BROADCAST_RECEIVER: required to read modules data -->
    <uses-permission android:name="org.mtransit.android.receiver.permission.BROADCAST_RECEIVER" />


    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <activity android:name="ru.astralight.koksharov.organizer.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="ru.astralight.koksharov.organizer.ui.TaskAddActivity">
        </activity>
        <activity android:name="ru.astralight.koksharov.organizer.ui.TaskDistribution">
        </activity>
        <activity android:name="ru.astralight.koksharov.organizer.ui.Statistics">
        </activity>
    </application>

</manifest>